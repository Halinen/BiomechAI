"""
config.py — 全局配置
所有路径、模型名、参数都集中在这里，改一处全局生效。
"""

from pathlib import Path

# ── 路径 ──────────────────────────────────────────────────────────────────────

# 知识库文本文件所在目录
KB_PATH = Path("e:/Fitting/knowledge_base")

# Chroma 向量数据库存储目录（会自动创建）
CHROMA_PATH = Path("e:/Fitting/chroma_db")

# ── 向量化模型 ────────────────────────────────────────────────────────────────
# all-MiniLM-L6-v2：轻量、速度快、英文效果好，首次运行会自动下载（约90MB）
# 后期换更好的模型只需改这一行，其余代码不动
EMBEDDING_MODEL = "all-MiniLM-L6-v2"

# ── 集合（Collections）────────────────────────────────────────────────────────
# 每个文件夹对应 Chroma 里的一个独立集合，检索时互不干扰
# key = 知识库子文件夹名，value = Chroma 集合名（小写，不能有空格）
COLLECTIONS = {
    "FMS_SFMA":              "fms_sfma",              # 功能动作筛查 / SFMA
    "NASM_CES":              "nasm_ces",              # 代偿模式 / 纠正练习
    "PRI":                   "pri",                   # 呼吸 / 肋廓 / 骨盆不对称
    "Red_Flags":             "red_flags",             # 红旗症状 / 转介标准
    "Visceral_Fascia":       "visceral_fascia",       # 内脏筋膜 / 术后粘连 / 内脏手法
    "Pain_Neuroscience":     "pain_neuroscience",     # 疼痛神经科学 / 中枢敏化 / PNE
    "Breathing_Retraining":  "breathing_retraining",  # 呼吸重训 / DNS / 膈肌功能
}

# ── 文本分块参数 ──────────────────────────────────────────────────────────────
# CHUNK_SIZE：每个"块"的目标字符数（约 300~500 是常见取值）
# 太大 → 检索不精准；太小 → 上下文不够，Claude 答不好
CHUNK_SIZE = 450

# CHUNK_OVERLAP：相邻块之间重叠的字符数
# 重叠可以避免重要信息恰好被切断在块边界
CHUNK_OVERLAP = 80

# ── 检索参数 ──────────────────────────────────────────────────────────────────
# 每次查询从每个集合里取多少个最相关的块
TOP_K = 5

# ── 相似度过滤阈值（余弦距离，越小越相似）────────────────────────────────────
# 这套阈值是按"中文 query 翻译成英文后"在各 collection 的实测距离分布调的：
#   pain_neuroscience: 0.44-0.47（全部命中）
#   pri:               0.45-0.50（全部命中）
#   nasm_ces:          0.48-0.57（前 5 命中）
#   fms_sfma:          0.54-0.65（边界稍高）
# 设 ACCEPT=0.65：~85% 的 chunk 直接保留（包括 fms_sfma 大部分），跳过 LLM judge。
# REJECT=0.72：兜底丢明显不相关；中间区域窄，judge 触发次数大幅下降。
# 演进：0.35/0.60 → 0.50/0.65 → 0.60/0.70 → 0.65/0.72
# 每次按实测调一档，最终把单 query judge 次数从 30+ 砍到 ~5。
SIMILARITY_ACCEPT = 0.65  # 低于此值：直接保留（高置信度）
SIMILARITY_REJECT = 0.72  # 高于此值：直接丢弃（低置信度）
# 介于两者之间：交给 LLM 判断（边界情况）

# 是否在边界距离区调 LLM judge：
#   关闭后边界 chunk 直接保留（信任向量距离），不调 LLM。
#   实测每条 query 烧 10+ 次 LLM judge 调用是 token 黑洞，免费档单条 case 就把日配额耗掉。
#   关掉后单 case token 从 ~3.5k 降到 ~1k，可以完整跑 15 条 eval。
#   trade-off: 边界 chunk 质量略降，但翻译层已确保大部分 chunk 距离 < 0.65 直接保留。
ENABLE_LLM_JUDGE = False

# Critic agent 自我反思最大迭代轮数（生成→质检→不合格则重生成；超出仍不通过则兜底输出）
MAX_CRITIC_ITERATIONS = 2

# ── LLM 模型 ─────────────────────────────────────────────────────────────────
# Groq 免费模型，速度极快
CLAUDE_MODEL = "llama-3.3-70b-versatile"
GROQ_BASE_URL = "https://api.groq.com/openai/v1"

# ── 三层评估框架 ──────────────────────────────────────────────────────────────
# 这是系统的核心分类逻辑，也是 prompt 里引导 Claude 的依据
LAYER_DESCRIPTIONS = {
    "控制层": (
        "神经肌肉控制层 — 问题根源在运动模式的神经调控，"
        "例如：呼吸模式异常、骨盆位置感知缺失、肌肉激活顺序错误。"
        "对应知识库：PRI（呼吸、骨盆不对称）、Breathing_Retraining（DNS/膈肌重训）"
    ),
    "结构层": (
        "关节/组织结构层 — 问题根源在关节活动度、软组织长度或骨性限制，"
        "例如：髋关节活动受限、胸椎僵硬、足弓塌陷、术后内脏粘连牵拉体壁。"
        "对应知识库：FMS/SFMA（动作筛查发现的受限模式）、Visceral_Fascia（内脏筋膜粘连）"
    ),
    "输出层": (
        "动作输出层 — 问题表现为肌肉失衡和代偿动作，"
        "例如：上交叉综合征、髋屈肌过度激活、臀肌抑制。"
        "对应知识库：NASM CES（抑制/延展/激活/整合流程）"
    ),
    "神经敏化层": (
        "疼痛神经科学层 — 问题根源在中枢敏化或外周痛觉过敏，"
        "例如：按压触发全身反应、疼痛与组织损伤不成比例、恐惧回避行为。"
        "对应知识库：Pain_Neuroscience（PNE/中枢敏化/神经动员）"
    ),
}
