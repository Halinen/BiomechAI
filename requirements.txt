# RAG 系统核心依赖
# 安装命令：pip install -r requirements.txt

# ── LLM API（Groq 走 OpenAI 兼容接口；anthropic 留作 Claude API 后备）────────
openai>=1.50.0
anthropic>=0.40.0

# ── 向量数据库 ──────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── 嵌入模型 ────────────────────────────────────────────────────────────────
sentence-transformers>=3.0.0

# ── Web UI ──────────────────────────────────────────────────────────────────
gradio>=4.0.0

# ── 环境变量管理 ────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── 图片处理（Gradio 图片上传需要）──────────────────────────────────────────
Pillow>=10.0.0

# ── Eval 流水线 ─────────────────────────────────────────────────────────────
PyYAML>=6.0
rich>=13.0
